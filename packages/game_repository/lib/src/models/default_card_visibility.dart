/// Defines the default visibility of cards in a game.
enum DefaultCardVisibility {
  /// Cards are only visible to the owner.
  owner,

  /// Cards are visible to all players.
  all,

  /// Cards are not visible to any player.
  none,
}
