name: game_repository
description: A Very Good Project created by Very Good CLI.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: 3.10.0

dependencies:
  equatable: ^2.0.5
  firebase_database: ^10.2.5
  flutter:
    sdk: flutter
  lobby_repository:
    path: ../lobby_repository

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^0.3.0
  very_good_analysis: ^5.0.0
